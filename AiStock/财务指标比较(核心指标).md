# 12个核心指标（覆盖关键维度 + 避免冗余 + 数据可得性强 + 抗操纵性高），行业对标分析


## 📊 第一步：精选12个核心指标（Code + cnName + 逻辑说明）

| Code     | cnName（中文名称）                  | 维度         | 为什么选它？ |
|----------|-------------------------------------|--------------|-------------|
| **col197** | 净资产收益率                         | 盈利能力     | 衡量股东回报核心指标，杜邦分析基石 |
| **col202** | 销售毛利率(%)                        | 盈利能力     | 反映产品/服务定价权与竞争壁垒 |
| **col107** | 经营活动产生的现金流量净额           | 现金流质量   | “造血能力”硬指标，难以操纵 |
| **col228** | 经营活动现金流净额 / 净利润比率       | 现金流质量   | 利润含金量，>1为优 |
| **col210** | 资产负债率(%)                        | 偿债安全     | 杠杆水平，过高=财务风险 |
| **col159** | 流动比率                             | 偿债安全     | 短期偿债能力基本保障 |
| **col175** | 总资产周转率                         | 运营效率     | 全资产使用效率，综合运营水平 |
| **col172** | 应收账款周转率                       | 运营效率     | 回款速度，影响现金流与坏账风险 |
| **col183** | 营业收入增长率(%)                    | 成长性       | 市场扩张能力核心指标 |
| **col184** | 净利润增长率(%)                      | 成长性       | 盈利增长速度（需结合现金流验证） |
| **col1**   | 基本每股收益                         | 股东回报     | 最直观的股东收益指标 |
| **col247** | 机构持股总量(股)                     | 市场认可度   | 专业投资者用真金白银投票 |

> ✅ **排除说明**：
> - 未选“速动比率”（与流动比率高度相关）
> - 未选“ROA”（与ROE、总资产周转率信息重叠）
> - 未选“扣非净利润”（已通过现金流比率验证利润质量）

---

## ⚖️ 第二步：标准化评分规则（Z-Score + Min-Max 双保险）

为避免异常值干扰，采用 **“Winsorize + Min-Max 标准化”**：

- **正向指标**（越大越好）：如ROE、毛利率、现金流、周转率、增长率等 → **标准化后得分 = (x - min) / (max - min)**
- **负向指标**（越小越好）：如资产负债率 → **标准化后得分 = (max - x) / (max - min)**

> ✅ 使用 **5% Winsorize**（上下5%缩尾）提升鲁棒性

---

## 🐍 第三步：Python实现（财务健康度评分模型）

```python
import pandas as pd
import numpy as np
import plotly.express as px
from scipy.stats.mstats import winsorize

# 假设你有一个DataFrame `df`，包含多家同行业公司的财务数据
# 列名为 col1, col107, col159, ..., col247
# index为公司名称或股票代码

# === 1. 定义12个核心指标 ===
metrics = {
    'Profitability': ['col197', 'col202'],
    'CashFlow': ['col107', 'col228'],
    'Solvency': ['col210', 'col159'],
    'Efficiency': ['col175', 'col172'],
    'Growth': ['col183', 'col184'],
    'Shareholder': ['col1', 'col247']
}

# 扁平化指标列表
all_cols = [col for cols in metrics.values() for col in cols]

# === 2. 数据预处理：Winsorize + 处理负向指标 ===
df_clean = df[all_cols].copy()

# 对每个指标进行5%缩尾处理（防止极端值干扰）
for col in all_cols:
    if col != 'col210':  # col210（资产负债率）已是百分比，通常无极端值，也可处理
        df_clean[col] = winsorize(df_clean[col].fillna(0), limits=[0.05, 0.05])

# 负向指标：资产负债率（col210）需取反
df_clean['col210_inv'] = -df_clean['col210']
all_cols_score = [col if col != 'col210' else 'col210_inv' for col in all_cols]

# === 3. Min-Max 标准化（0~1分）===
df_score = pd.DataFrame(index=df.index)
for col in all_cols:
    col_for_score = col if col != 'col210' else 'col210_inv'
    min_val = df_clean[col_for_score].min()
    max_val = df_clean[col_for_score].max()
    if max_val == min_val:
        df_score[col] = 0.5  # 所有公司相同，给中等分
    else:
        if col == 'col210':  # 负向指标用反向值标准化
            df_score[col] = (df_clean['col210_inv'] - min_val) / (max_val - min_val)
        else:
            df_score[col] = (df_clean[col] - min_val) / (max_val - min_val)

# === 4. 计算维度得分与总分 ===
dimension_scores = {}
for dim, cols in metrics.items():
    dimension_scores[dim] = df_score[cols].mean(axis=1)

# 总分 = 各维度平均（可加权，此处等权）
df_score['Total_Score'] = pd.DataFrame(dimension_scores).mean(axis=1)

# 按总分降序排名
df_score['Rank'] = df_score['Total_Score'].rank(ascending=False, method='min')

# === 5. 重命名列名（方便展示）===
col_rename = {
    'col197': 'ROE',
    'col202': '毛利率',
    'col107': '经营现金流',
    'col228': '现金流/净利润',
    'col210': '资产负债率',
    'col159': '流动比率',
    'col175': '总资产周转率',
    'col172': '应收周转率',
    'col183': '营收增长率',
    'col184': '净利润增长率',
    'col1': '每股收益',
    'col247': '机构持股量',
    'Total_Score': '综合健康度',
    'Rank': '排名'
}

df_final = df_score[['Total_Score', 'Rank'] + all_cols].rename(columns=col_rename)

# === 6. 可视化：综合健康度排名 ===
fig = px.bar(
    df_final.sort_values('综合健康度', ascending=False).reset_index(),
    x='index', y='综合健康度',
    title='同行业公司财务健康度排名（12项核心指标）',
    labels={'index': '公司', '综合健康度': '健康度评分（0~1）'},
    color='综合健康度',
    color_continuous_scale='RdYlGn'
)
fig.update_layout(xaxis_tickangle=45)
fig.show()

# === 7. 输出结果 ===
print(df_final.sort_values('综合健康度', ascending=False)[['综合健康度', '排名'] + list(col_rename.values())[:12]])
```

---

## 📈 第四步：模型输出示例（假设数据）

| 公司   | 综合健康度 | 排名 | ROE | 毛利率 | ... | 资产负债率 | 机构持股量 |
|--------|------------|------|-----|--------|-----|------------|------------|
| 公司A  | 0.87       | 1    | 0.92| 0.85   | ... | 0.78       | 0.90       |
| 公司B  | 0.76       | 2    | 0.80| 0.70   | ... | 0.85       | 0.80       |
| ...    | ...        | ...  | ... | ...    | ... | ...        | ...        |

> ✅ **解读**：
> - 公司A在盈利、现金流、机构认可度上全面领先。
> - 若某公司“净利润增长率”高但“现金流/净利润”<0.5，说明增长质量差，会被模型自动降分。

---

## 🔧 第五步：模型优化建议（进阶）

1. **动态权重**：根据行业特性调整维度权重  
   - 制造业：提高“运营效率”权重  
   - 消费品：提高“盈利能力+成长性”权重  
   - 金融：替换为资本充足率、不良率等

2. **加入预警项**：  
   - 若 `col228 < 0.5`（现金流/净利润过低），总分乘以0.9  
   - 若 `col210 > 70%`（高负债），偿债维度得分上限0.6

3. **时间序列扩展**：  
   - 计算3年平均得分，避免单年波动  
   - 增加“趋势得分”（如ROE连续3年上升 +0.1分）

4. **对标行业均值**：  
   - 得分 = (公司值 - 行业均值) / 行业标准差 （Z-Score）  
   - 可识别“相对行业”的强弱

---

## ✅ 总结

这个 **12指标财务健康度模型** 具备以下优势：

- **全面性**：覆盖5大核心维度  
- **抗操纵**：强调现金流、周转率等硬指标  
- **可解释**：每个维度可拆解归因  
- **可扩展**：支持加权、预警、动态调整  
- **可视化强**：一键生成排名图、雷达图（可扩展）

> 💡 **下一步**：将此模型嵌入你的行业分析报告，或作为股票筛选器的底层逻辑。

如需我帮你：
- 将此模型适配到具体行业（如新能源、医药、银行）
- 生成 **雷达图** 展示各公司五维能力
- 导出为 Excel 模板（含公式）

请随时告诉我！

