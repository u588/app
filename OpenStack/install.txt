https://www.dianjilingqu.com/406988.html

Controller --> Identity service, Image service, Placement service
Compute    --> Networking service agent 
Block Storage --> 


================ 网络 =============
vi /etc/netplan/00-installer-config.yaml
netplan apply

hostnamectl set-hostname  xxx


1: The archive enablement described here needs to be done on all nodes that run OpenStack services.

    Archive Enablement: 
        add-apt-repository cloud-archive:antelope
        apt install nova-compute
        apt install python3-openstackclient
        apt install apache2


================ master ============
Message queue
    apt install rabbitmq-server
    rabbitmqctl add_user openstack syslog6^
查看凭据    rabbitmqctl set_permissions openstack ".*" ".*" ".*"

 * node name: 'rabbitmqcli-393-rabbit@os-master-86'
 * effective user's home directory: /var/lib/rabbitmq
 * Erlang cookie hash: egKmxKdVbOMJw3HqzeK7MQ==


Memcached
    apt install memcached python3-memcache
    -->/etc/memcached.conf  -l 10.145.254.86
    service memcached restart


Etcd
    apt install etcd
     /etc/default/etcd  -->  

ETCD_NAME="os-master-86"
ETCD_DATA_DIR="/var/lib/etcd"
ETCD_INITIAL_CLUSTER_STATE="new"
ETCD_INITIAL_CLUSTER_TOKEN="etcd-os-master-86"
ETCD_INITIAL_CLUSTER="os-master-86=http://10.145.254.86:2380"
ETCD_INITIAL_ADVERTISE_PEER_URLS="http://10.145.254.86:2380"
ETCD_ADVERTISE_CLIENT_URLS="http://10.145.254.86:2379"
ETCD_LISTEN_PEER_URLS="http://0.0.0.0:2380"
ETCD_LISTEN_CLIENT_URLS="http://10.145.254.86:2379"

systemctl enable etcd
systemctl restart etcd

keystone
    apt install keystone

cat >   /etc/keystone/keystone.conf <<EOF
[database]  
connection = postgresql+psycopg2://sa:11111111@10.145.254.56/keystone
[token]
provider = fernet
EOF



    su -s /bin/sh -c "keystone-manage db_sync" keystone
    keystone-manage fernet_setup --keystone-user keystone --keystone-group keystone
    keystone-manage credential_setup --keystone-user keystone --keystone-group keystone

keystone-manage bootstrap --bootstrap-password syslog6^ \
  --bootstrap-admin-url http://10.3.18.86:5000/v3/ \
  --bootstrap-internal-url http://10.145.254.86:5000/v3/ \
  --bootstrap-public-url http://10.3.18.86:5000/v3/ \
  --bootstrap-region-id Os-86

/etc/apache2/apache2.conf --> ServerName 10.3.18.86

service apache2 restart

export OS_USERNAME=admin
export OS_PASSWORD=syslog6^
export OS_PROJECT_NAME=admin
export OS_USER_DOMAIN_NAME=Default
export OS_PROJECT_DOMAIN_NAME=Default
export OS_AUTH_URL=http://10.3.18.86:5000/v3
export OS_IDENTITY_API_VERSION=3


Image service



openstack user create --domain default --password-prompt glance # 这里要输入密码，密码也设置成glance
openstack role add --project service --user glance admin
openstack service create --name glance --description "OpenStack Image" image
openstack endpoint create --region Os-86 image public http://10.3.18.86:9292
openstack endpoint create --region Os-86 image internal http://10.145.254.86:9292
openstack endpoint create --region Os-86 image admin http://10.3.18.86:9292






Glance安装
    apt install glance

cat >   /etc/glance/glance-api.conf <<EOF
[database]
connection = postgresql+psycopg2://sa:11111111@10.145.254.56/glance
[keystone_authtoken]
www_authenticate_uri = http://os-master-86:5000
auth_url = http://os-master-86:5000
memcached_servers = os-master-86:11211
auth_type = password
project_domain_name = Default
user_domain_name = Default
project_name = service
username = glance
password = syslog6^
[paste_deploy]
flavor = keystone
[glance_store]
stores = file,http
default_store = file
filesystem_store_datadir = /var/lib/glance/images/      
EOF
    
    
    
    [oslo_limit]
        auth_url = http://controller:5000
        auth_type = password
        user_domain_id = default
        username = MY_SERVICE
        system_scope = all
        password = MY_PASSWORD
        endpoint_id = ENDPOINT_ID
        region_name = RegionOne    

su -s /bin/sh -c "glance-manage db_sync" glance
ervice glance-api restart



Placement安装
. ~/openrc/admin-openrc
openstack user create --domain default --password-prompt placement # 设置密码也是placement
openstack role add --project service --user placement admin # 将admin用户添加到placement
openstack service create --name placement --description "Placement API" placement
openstack endpoint create --region Os-86 placement public http://10.3.18.86:8778
openstack endpoint create --region Os-86 placement internal http://10.145.254.86:8778
openstack endpoint create --region Os-86 placement admin http://10.3.18.86:8778

apt install placement-api

cat > vim /etc/placement/placement.conf <<EOF
[placement_database]
# ...
connection = postgresql+psycopg2://sa:11111111@10.145.254.56/placement
[api]
# ...
auth_strategy = keystone
[keystone_authtoken]
# ...
auth_url = http://os-master-86:5000/v3
memcached_servers = os-master-86:11211
auth_type = password
project_domain_name = Default
user_domain_name = Default
project_name = service
username = placement
password = syslog6^

EOF

su -s /bin/sh -c "placement-manage db sync" placement
service apache2 restart

pip3 install osc-placement -i https://mirrors.aliyun.com/pypi/simple/



Nova安装

openstack user create --domain default --password-prompt nova # 这里设置nova用户的密码也是nova
openstack role add --project service --user nova admin  # 将nova用户添加到admin组中变成管理员
openstack service create --name nova --description "OpenStack Compute" compute # 创建服务实体
openstack endpoint create --region Os-86 compute public http://10.3.18.86:8774/v2.1 # 提供API服务
openstack endpoint create --region Os-86 compute internal http://10.145.254.86:8774/v2.1
openstack endpoint create --region Os-86 compute admin http://10.3.18.86:8774/v2.1


apt -y install nova-api nova-conductor nova-novncproxy nova-scheduler


cat > /etc/nova/nova.conf <<EOF
[DEFAULT]
log_dir = /var/log/nova
lock_path = /var/lock/nova
state_path = /var/lib/nova

my_ip = 10.145.254.86
transport_url = rabbit://openstack:syslog6^@os-master-86:5672/

[api_database]
# ...该组中已有的配置全部注释掉
connection = postgresql+psycopg2://sa:11111111@10.145.254.56/nova_api

[database]
# ...该组中已有的配置全部注释掉
connection = postgresql+psycopg2://sa:11111111@10.145.254.56/nova

[api]
# ...该组中已有的配置全部注释掉
auth_strategy = keystone

[keystone_authtoken]
# ...该组中已有的配置全部注释掉
www_authenticate_uri = http://os-master-86:5000/
auth_url = http://os-master-86:5000/
memcached_servers = os-master-86:11211
auth_type = password
project_domain_name = Default
user_domain_name = Default
project_name = service
username = nova
password = syslog6^

[vnc]
# ...
enabled = true
server_listen = $my_ip
server_proxyclient_address = $my_ip

[glance]
# ...
api_servers = http://os-master-86:9292

[oslo_concurrency]
# ...
lock_path = /var/lib/nova/tmp

[placement]
# ...
region_name = Os-86
project_domain_name = Default
project_name = service
auth_type = password
user_domain_name = Default
auth_url = http://os-master-86:5000/v3
username = placement
password = syslog6^
[cells]
enable = False
[os_region_name]
openstack =

EOF


su -s /bin/sh -c "nova-manage api_db sync" nova
su -s /bin/sh -c "nova-manage cell_v2 map_cell0" nova
su -s /bin/sh -c "nova-manage cell_v2 create_cell --name=cell1 --verbose" nova
su -s /bin/sh -c "nova-manage db sync" nova




============== node =============

apt -y install nova-compute



 cat > /etc/nova/nova.conf  <<EOF
[DEFAULT]
# ...
transport_url = rabbit://openstack:syslog6^@os-master-86
my_ip = 10.145.254.82

[api]
# ...
auth_strategy = keystone

[keystone_authtoken]
# ...
www_authenticate_uri = http://os-master-86:5000/
auth_url = http://os-master-86:5000/
memcached_servers = os-master-86:11211
auth_type = password
project_domain_name = Default
user_domain_name = Default
project_name = service
username = nova
password = syslog6^

[vnc]
# ...
enabled = true
server_listen = 0.0.0.0
server_proxyclient_address = $my_ip
novncproxy_base_url = http://os-master-86:6080/vnc_auto.html

[glance]
# ...
api_servers = http://os-master-86:9292

[oslo_concurrency]
# ...
lock_path = /var/lib/nova/tmp

[placement]
# ...
region_name = Os-86
project_domain_name = Default
project_name = service
auth_type = password
user_domain_name = Default
auth_url = http://os-master-86:5000/v3
username = placement
password = syslog6^

EOF



vi /etc/nova/nova-compute.conf --> virt_type = qemu



Neutron安装

. ~/openrc/admin-openrc
openstack user create --domain default --password-prompt neutron # 这里设置密码，密码设成neutron，方便记忆
openstack role add --project service --user neutron admin # 把neutron用户加到admin组
openstack service create --name neutron --description "OpenStack Networking" network # 实例化服务
openstack endpoint create --region Os-86 network public http://os-master-86:9696 # 老样子，创建3大接口
openstack endpoint create --region Os-86 network internal http://os-master-86:9696
openstack endpoint create --region Os-86 network admin http://os-master-86:9696


apt -y install neutron-server neutron-plugin-ml2 neutron-linuxbridge-agent neutron-l3-agent neutron-dhcp-agent neutron-metadata-agent

cat > /etc/neutron/neutron.conf <<EOF
[DEFAULT]
# ...
core_plugin = ml2
service_plugins = router
allow_overlapping_ips = true
transport_url = rabbit://openstack:syslog6^@os-master-86
auth_strategy = keystone
notify_nova_on_port_status_changes = true
notify_nova_on_port_data_changes = true

[database]
# ...database组中已有的配置注释掉
connection = postgresql+psycopg2://sa:11111111@10.145.254.56/neutron

[keystone_authtoken]
# ...
www_authenticate_uri = http://os-master-86:5000
auth_url = http://os-master-86:5000
memcached_servers = os-master-86:11211
auth_type = password
project_domain_name = default
user_domain_name = default
project_name = service
username = neutron
password = syslog6^

[nova]
# ...
auth_url = http://os-master-86:5000
auth_type = password
project_domain_name = default
user_domain_name = default
region_name = Os-86
project_name = service
username = nova
password = syslog6^

[oslo_concurrency]
# ...
lock_path = /var/lib/neutron/tmp
EOF


cat > /etc/neutron/plugins/ml2/ml2_conf.ini  <<EOF
[ml2]
# ...
type_drivers = flat,vlan,vxlan
tenant_network_types = vxlan
mechanism_drivers = linuxbridge,l2population
extension_drivers = port_security

[ml2_type_flat]
# ...
flat_networks = provider

[ml2_type_vxlan]
# ...
vni_ranges = 1:1000 #VID 

[securitygroup]
# ...
enable_ipset = true
EOF


cat > /etc/neutron/plugins/ml2/linuxbridge_agent.ini <<EOF
-----------------------------------------------------
[linux_bridge]
physical_interface_mappings = provider:ens16 # 这里的ens19是我的203.0.113.0/24网段的网口名称，你需要根据你自己的实际填写，不能照抄我的。
# provider 网络可以理解为能与外部互联网相通的网络，后面在创建Flat 类型网络时 --provider-physical-network 要指定是provider。


[vxlan]
enable_vxlan = true
local_ip = 10.0.20.190 # manage ip,要修改成控制节点/计算节点实际用于vxlan IP。
l2_population = true

[securitygroup]
# ...
enable_security_group = true
firewall_driver = neutron.agent.linux.iptables_firewall.IptablesFirewallDriver


Horizon安装

apt -y install openstack-dashboard

cat > /etc/openstack-dashboard/local_settings.py <<EOF
OPENSTACK_HOST = "os-master-86"
ALLOWED_HOSTS = ['*'] # *表示允许任何外部主机访问Horizon，但是这样不安全，生产环境请写几台机器用户访问Horizon。
SESSION_ENGINE = 'django.contrib.sessions.backends.cache'
CACHES = {
    'default': {
         'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
         'LOCATION': 'os-master-86:11211',
    }
}
OPENSTACK_KEYSTONE_URL = "http://%s:5000/identity/v3" % OPENSTACK_HOST # 这个是Python的语法，字符串格式化输出。

OPENSTACK_KEYSTONE_MULTIDOMAIN_SUPPORT = True
OPENSTACK_API_VERSIONS = {
    "identity": 3,
    "image": 2,
    "volume": 3,
}
OPENSTACK_KEYSTONE_DEFAULT_DOMAIN = "Default"
OPENSTACK_KEYSTONE_DEFAULT_ROLE = "user" # 新注册用户的默认角色和权限为普通用户
TIME_ZONE = "Asia/Shanghai"

EOF

sudo vim /etc/apache2/conf-available/openstack-dashboard.conf
WSGIApplicationGroup %{GLOBAL}
